import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  headerContainer: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
  titleStyle: {
    paddingLeft: 10,
    fontSize: 20,
  },
});

export default styles;
