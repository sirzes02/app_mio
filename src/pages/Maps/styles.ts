import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  MainContainer: {
    display: 'flex',
  },
  mapStyle: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
